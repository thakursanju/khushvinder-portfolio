Drop your audio files here. Filenames referenced by the components:

  the-way.mp3   — track played by the PlaylistCard vinyl

The PlaylistCard plays whatever file is at /audio/the-way.mp3. You need to
supply this file yourself (the component ships no audio).

Browser autoplay policy: the track will only start after a user click. The
click-to-play interaction on the vinyl satisfies this on every modern browser.
